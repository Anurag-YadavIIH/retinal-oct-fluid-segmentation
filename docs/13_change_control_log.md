<!--
Document: Change Control Log
Status: LIVING — updated per commit
Owner: Anurag Yadav
Last reviewed: 2026-09-16
Change history: this document
-->

# Change Control Log

Any change to a requirement, hazard, risk control, evaluation definition, or
frozen contract is recorded here with a date and rationale. Metrics never change
silently (CLAUDE.md rule 6).

| Date | Change | Affected IDs | Rationale | Author |
|---|---|---|---|---|
| 2026-09-16 | Repository scaffolded; document set created as templates | — | Project initiation | Anurag Yadav |
| 2026-09-16 | Working tree placed under version control (`git init`, initial commit `945705d`). Milestone 1 status in `CLAUDE.md` §7 qualified: the recorded run (5 passed / 1 xfailed, ruff clean) pre-dates the repository, so no commit carried it and CI had never run. Result re-confirmed from inside the repo. | — | The scaffolding was complete but untracked, so no change to it was attributable or reversible, and rule 5 had no commit boundary to attach to | Anurag Yadav |
| 2026-09-16 | `docs/06_data_management_plan.md` drafted to v0.1, replacing the template (commit `56a30b3`). Establishes DMP-C1..C10 and records the §2.2 licence interpretation as a judgment. Drafted ahead of `docs/01`-`03`, out of milestone order. | DMP-C1..DMP-C10 | The §2 licence analysis is a go/no-go gate on RETOUCH and had to be answered before committing weeks of work to the dataset. No code depends on the document, so rule 1 is not engaged. Five open items remain in §10; items 2 and 3 need the data downloaded | Anurag Yadav |
| 2026-09-16 | `docs/01_intended_use.md` drafted to v0.1, replacing the template. Allocates URS-001..URS-010 and states limitations L1..L7. Intended user fixed as a supervised reading-centre grader; workflow position fixed as pre-read in a retrospective research context. `docs/09` updated in the same commit per rule 5. | URS-001..URS-010 | Milestone 2. Clinical context supplied by the author rather than assumed. Scope deliberately excludes treatment monitoring and longitudinal change: RETOUCH has one volume per subject and no repeat acquisitions, so repeatability is uncharacterised (L4) and any such claim would be unsupported | Anurag Yadav |
| 2026-09-16 | `docs/01` — URS-011 added on review: output objects must carry an in-object research-use-only designation. URS-003 rationale split — the factual statement about human review stays, the instruction to `docs/03` is replaced by a forward reference. URS-009 rationale records that its conformance criteria are unresolved. New §8 open items; `docs/09` now 11 rows. | URS-011 (new); URS-003, URS-009 (rationale) | §5 and the README do not travel with a SEG or SR object that is copied out of context, so URS-011 is the only designation that survives the artefact leaving the repository. It states the property, not the mechanism: no tag is named, because no single canonical mechanism exists and the choice is a real trade-off between visibility and resistance to stripping. Settled in `docs/11` before milestone 6. `docs/01` records what is true of the user; `docs/03` decides what follows from it | Anurag Yadav |
| 2026-09-16 | `docs/02_software_requirements_spec.md` drafted to v0.1, replacing the template. Allocates SRS-001..SRS-049 and NFR-001..NFR-008 across the nine subsystems, plus §3.10 mapping every module under `src/ocuval` to the requirements it implements. `docs/09` updated in the same commit: URS coverage now 11 of 11, SRS test coverage 4 of 49. | SRS-001..SRS-049, NFR-001..NFR-008 | Milestone 2. Section 3.10 exists so CLAUDE.md rule 1 is checkable rather than asserted. TC-000 is deliberately allocated to no requirement: it asserts only that a version identifier exists, which is a precondition of SRS-031 and SRS-049, not a verification of either — claiming otherwise would have put a false trace in the matrix | Anurag Yadav |
| 2026-09-16 | `docs/01` URS-010 rationale names the provenance/determinism seam: the two halves fail differently and verify differently — provenance by inspecting an artefact, determinism by running twice and diffing. Requirement not split. | URS-010 (rationale) | URS-010 derives 18 requirements, which raised the question of whether it is one requirement or two. Kept as one; the seam is recorded so `docs/07` can cut along it if verification proves unwieldy | Anurag Yadav |
| 2026-09-16 | UID root decision recorded: the configured root stays a declared placeholder and no root is registered. Written into `docs/06` §7.1 (new), `docs/11` §7 and §10, and the README. Closes `docs/02` §6 item 3. | SRS-007 | UIDs generated under an unregistered root are structurally valid but carry no claim of global uniqueness, so objects must not leave the local Orthanc instance. Substituting a different-looking root was rejected: a plausible unregistered root is worse than a declared one, because it invites the assumption that someone checked | Anurag Yadav |
| 2026-09-16 | `docs/03_safety_classification.md` drafted to v0.1, replacing the template. Walks IEC 62304 §4.3, identifies hazardous situations HS-1..HS-7 and external controls ERC-1..ERC-6, and **assigns software safety class B**. No segregation claimed; all software items inherit B. | HS-1..HS-7, ERC-1..ERC-6 (interim, local to `docs/03`) | Class A was available only by crediting mandatory grader review as fully mitigating, which §4.1 rejects: automation bias makes review weakest against the subtle, systematic errors that are both the most likely failure and the most damaging to a quantitative endpoint. Class C is not reached because the intended use interposes a grader and an aggregated study endpoint and forbids any individual treatment decision. Choosing B adds 62304 §5.3, §5.6, §5.7 and clause 7 to scope | Anurag Yadav |
| 2026-09-17 | `docs/03` amended: §6 expanded to carry the software unit verification subclauses (§5.5.1..§5.5.5) rather than a single 5.5 row, since 5.5.2 unit verification process, 5.5.3 unit acceptance criteria and 5.5.5 unit verification are Class B obligations that a single row concealed. New §1.2 states the sources the classification was developed against. Open items renumbered to 7; item 3 (unit-level criteria owed by `docs/07`), item 6 (enforcement test case) and item 7 (clause verification) are new. | — (no requirement, hazard or test changed) | §6 understated Class B scope: under Class A only 5.5.1 applies, so the added clauses are part of what choosing B actually costs, and a passing pytest suite does not discharge them. §1.2 was added because the classification was developed against the publicly documented clause structure and secondary literature, not the normative text — no licensed copy was available — and implying otherwise would be the same failure as inventing a DICOM tag | Anurag Yadav |
| 2026-09-17 | `docs/04_soup_list.md` drafted to v0.1, replacing the template. Retains SOUP-001..SOUP-014 and adds SOUP-015..SOUP-022, separating shipped components (§3) from the runtime environment (§4) and from development and test tools (§5), which are not SOUP. Every pinned version in `pyproject.toml` now appears. **No anomaly or advisory review has been performed for any component** (§2, open item 1). | SOUP-015..SOUP-022 (new) | The template omitted python-multipart, the Python runtime, the container base image and all four dev tools, so the list did not satisfy its own rule that every pin appears. Drafting it surfaced two defects recorded as open items: scikit-learn supports no allocated requirement because `docs/02` §3.6 has no classification arm, and the API container base image is pinned by mutable tag rather than digest, which defeats NFR-002 for container builds | Anurag Yadav |
| 2026-09-17 | `docs/02` §6 open item 6 added: the classification arm (sensitivity, specificity, AUROC) named in CLAUDE.md §5 has no SRS requirement. | — (gap recorded, no requirement changed) | Surfaced by `docs/04`: scikit-learn is pinned to serve an arm the SRS never specified. A pinned dependency with no requirement behind it is the inverse of rule 1 | Anurag Yadav |
| 2026-09-17 | `docs/03` §6: software system testing (5.7) corrected from "required for B, not for A" to required for all classes under A1:2015, and removed from the list of costs incurred by choosing Class B. Open item 7 upgraded to corroborated-but-unverified. | — (no requirement, hazard or test changed) | `docs/07` was owed regardless of the classification outcome, so counting 5.7 as a consequence of choosing B overstated the cost of the decision. §6 exists to state that cost accurately. The obligation is amendment-dependent — it follows from A1:2015, not the 2006 text | Anurag Yadav |
| 2026-09-17 | Container images pinned by digest: `python:3.11-slim` and `orthancteam/orthanc:24.7.3`, digests resolved from the registry and recorded against SOUP-017 and SOUP-014. Closes `docs/04` open item 3. | SOUP-014, SOUP-017 | A tag is mutable and resolves to different image contents over time, defeating reproducible container builds against NFR-002 | Anurag Yadav |
| 2026-09-17 | SOUP anomaly review performed against OSV for the 14 shipped components, at exact pinned versions. Four are affected in ways bearing on allocated requirements: PyTorch 2.3.1 and MONAI 1.3.2 (deserialisation on the model-loading path), requests 2.32.3 (netrc leak, conditional), python-multipart 0.0.9 (upload DoS on SRS-046). No pin changed. Closes `docs/04` open item 1; opens items 7 and 8. | SOUP-001..SOUP-015 (review status) | Class B obliges evaluation of published anomaly lists. Review scoped to shipped components only and time-boxed: for the three components with large advisory sets it characterises the class of defect rather than asserting per-advisory coverage. Pins were not changed as a side effect — a pin change needs a fresh SOUP assessment and a compatibility decision, and PyTorch and MONAI move together | Anurag Yadav |
| 2026-09-17 | `docs/02` §3.6 extended with SRS-050..SRS-053: volume-level detection per fluid class, derived from the segmentation output. Presence when predicted voxel count exceeds a configured threshold; AUROC score from the MC-dropout mean probability; sensitivity, specificity and AUROC reported per class and per vendor with CI and n. `CLAUDE.md` §5 reworded so it no longer implies a separate classification arm. Closes `docs/02` open item 6 and `docs/04` open item 2. | SRS-050..SRS-053 (new) | scikit-learn was pinned to serve an arm the SRS never specified. Resolved by writing the requirements rather than dropping the dependency. SRS-050 fixes the derivation explicitly so nothing here can be read as authorising a second model or a second training run, and SRS-035's prohibition on accuracy applies to detection too | Anurag Yadav |
| 2026-09-17 | `docs/05_risk_management_file.md` drafted to v0.1, replacing the template. Allocates HAZ-001..HAZ-011 and RC-001..RC-020, superseding HS-1..HS-7 (`docs/03`) and DMP-C1..DMP-C10 (`docs/06`); §3.1 and §4.1 carry the mappings. Probability decomposed into P1 (taken as 1 throughout) and P2 (estimated). `docs/09` HAZ and RC columns now derived; `docs/06` §10 item 5 closed. | HAZ-001..HAZ-011, RC-001..RC-020 (new) | Taking IEC 62304's 100% software failure probability into risk estimation directly would make every probability identical and the analysis useless, so P1 is fixed at 1 and only P2 is estimated — no row reduces risk by claiming the code is reliable. RC-017 (human review) is typed Information, the weakest level of the ISO 14971 hierarchy, so no hazard reaches an acceptable residual on its strength alone. HAZ-005 carries the highest residual: the only control that can detect it is inside the software whose failure is assumed. Deriving `docs/09` surfaced two analysis gaps — RC-016 controlled no hazard (now linked to HAZ-005) and URS-002 has no control at all (open item 5) | Anurag Yadav |
| 2026-09-17 | **HAZ-012 added** — acquisition metadata corruption, distinct from segmentation error. Controls RC-021..RC-026 and requirements SRS-054..SRS-059: spacing carried first-class, presence asserted with no default permitted, per-vendor plausibility range that rejects rather than warns, spacing asserted bit-identical between ingestion and volume computation, units declared explicitly in the SR, voxel count and voxel volume recorded alongside mm³. Closes `docs/05` open item 5. | HAZ-012, RC-021..RC-026, SRS-054..SRS-059 | mm³ = voxel count × voxel volume, and voxel volume comes entirely from spacing metadata that no existing control touched. A correct mask with wrong spacing yields a confidently wrong volume that looks right on review, so RC-017 catches nothing. The OCT-specific case is a 1000× error: axial resolution is conventionally quoted in micrometres, so 0.0039 mm arriving as 3.9 is invisible in the mask and to the grader. SRS-010 and SRS-026 forbid harmonising the image but say nothing about the metadata | Anurag Yadav |
| 2026-09-17 | SOUP dispositions recorded (`docs/04` §2.4, §2.5). python-multipart bumped 0.0.9 → 0.0.31 (remediated). requests remediated in code by RC-027 / SRS-060 with the pin unchanged. PyTorch 2.3.1 and MONAI 1.3.2 **accepted** on the basis of RC-028 / SRS-061, with HAZ-013. HAZ-014 added for ambient client configuration. Closes `docs/04` open items 7 and 8 and `docs/05` open item 6. | SOUP-015 (version), SRS-060, SRS-061, RC-027, RC-028, HAZ-013, HAZ-014 | Identifying an anomaly does not oblige a bump; the obligation is to assess effect in the intended use and record the rationale. python-multipart had no in-code mitigation and hits SRS-046 directly, so remediation. requests was fixed in code instead because `trust_env=False` is strictly broader than the patch — it removes ambient netrc, proxy and certificate pickup, serving NFR-002 as well — and is verifiable by a test we own. PyTorch and MONAI were accepted because both advisories concern untrusted model files and this system loads only hash-verified checkpoints from its own artifacts directory; a coupled stack bump to chase a threat the intended use excludes is the wrong trade | Anurag Yadav |
| 2026-09-17 | `docs/07_vv_protocol.md` drafted to v0.1, replacing the template. Allocates 76 test cases TC-000..TC-113 across unit, integration, system and document-integrity levels. Carries the IEC 62304 §5.5.2 unit verification process and §5.5.3 acceptance criteria A1..A6, and the §5.6 integration strategy. Every SRS, NFR and RC now names a verifying test case; `docs/02` and `docs/05` back-filled, `docs/09` updated. Closes `docs/03` open items 2, 3 and 6 and allocates `docs/04` items 6 and 9. | TC-002..TC-113 (new); all SRS and RC test allocations | Class B obliges unit verification process, unit acceptance criteria and integration testing, none of which existed. Criterion A5 distinguishes testing a control from testing the feature it protects, which is the distinction a coverage percentage hides. TC-072 fixes the volume arithmetic against an anisotropic fixture with an independently computed expected value, and TC-015 requires the micrometre-valued spacing case to be rejected rather than warned — together these are the tests that would have caught HAZ-012. Coverage now measures allocation, not evidence: 3 of 76 cases are implemented | Anurag Yadav |
| 2026-09-17 | **Documentation paused at `docs/07` by decision.** `docs/08`, `docs/10`, `docs/11` and `docs/12` are not drafted and will not be until implementation supplies what they report on. | — | Each of the four reports something that does not exist yet: `docs/08` reports execution of `docs/07`, where 3 of 76 cases are implemented; `docs/10` needs results; `docs/11` needs DICOM output; `docs/12` needs a model. Drafting them now would add documents that say almost nothing and would worsen the ratio of claim to evidence, which is the failure mode this document set exists to avoid. **This is a decision, not an oversight** | Anurag Yadav |
| 2026-09-17 | RC-027 known limitation recorded (`docs/04` §2.6): `trust_env=False` also disables `HTTP_PROXY`, `HTTPS_PROXY`, `NO_PROXY` and `REQUESTS_CA_BUNDLE`, not only `.netrc`. Control unchanged. | RC-027, SRS-060 | Harmless against a localhost Orthanc and desirable here, but it would break the DICOMweb client behind the corporate proxy and internal CA that the reading centre in `docs/01` §4 would actually sit behind. Narrowing the control would lose the reproducibility property that justified choosing it over the patch bump, so the limitation is stated and the control kept | Anurag Yadav |
| 2026-09-17 | `eval/metrics.py` implemented: `dice`, `hd95`, `sensitivity_specificity`, `volume_mm3`, `bootstrap_ci` and the `Estimate` type. **Metric definitions fixed** (rule 6): Dice returns 1.0 when both masks are empty; HD95 returns 0.0 when both are empty and **NaN — not inf — when exactly one is**; sensitivity and specificity return NaN over an empty denominator; `bootstrap_ci` excludes NaN inputs and reports the n actually used. SRS-040 and SRS-059 additionally allocated to `eval/metrics.py`. TC-002, TC-060, TC-061, TC-062, TC-068 and TC-072 implemented. | SRS-032..SRS-035, SRS-040, SRS-053, SRS-059; RC-006, RC-007, RC-024, RC-026 | The empty-mask cases are undefined rather than zero and `docs/04` SOUP-007 flagged them as a decision not to inherit from a library default. NaN was chosen over inf because inf silently destroys any mean or bootstrap it enters, converting one degenerate case into a ruined aggregate, whereas NaN propagates visibly. The volume arithmetic was placed in `eval/metrics.py` so TC-072 can verify it against the anisotropic fixture without a DICOM runtime; `report/sr_object.py` still owns recording it | Anurag Yadav |
| 2026-09-17 | `data/splits.py` implemented: `Sample`, `Split`, `leave_one_vendor_out`, `assert_no_patient_overlap`, `save`, `load`. **The TC-004 xfail is removed and the leakage gate passes for real.** `Split` gains an `in_domain_ref` field (SRS-022) and the three fold configs gain `ref_fraction: 0.15`. TC-040, TC-041 and TC-042 implemented alongside. | SRS-018..SRS-023; RC-001, RC-002, RC-004, RC-005; HAZ-008 | Patients are the unit of assignment and slices never are, so slice-level leakage is impossible by construction rather than caught afterwards — which is what makes the assertion an independent check. Patient IDs are sorted before shuffling because set iteration order for strings is not stable across processes, so a seeded shuffle over an unsorted list is reproducible-looking and not reproducible. A patient appearing under two vendors now raises: it cannot happen in RETOUCH, which is exactly why assuming it would be unsafe | Anurag Yadav |
| 2026-09-17 | `io/deident.py` implemented (`pseudonymise`, `deidentify_instance`, `verify_deidentified`) and `io/retouch_reader.validate_spacing` added. Verified against DICOM instances generated with pydicom, not real data. TC-014, TC-015 and TC-030..TC-035 implemented. | SRS-012..SRS-017, SRS-055, SRS-056; RC-008, RC-011, RC-012, RC-022, RC-023 | The profile table is keyed by **DICOM keyword, never tag number**, so pydicom resolves every tag and this module cannot invent one — CLAUDE.md's most expensive failure mode removed by construction rather than by care. The table is a **subset** of Annex E and verification checks exactly that subset, so neither half detects an attribute the table omits; recorded as `docs/07` open item 7 and confirmed against `docs/04` SOUP-001. Spacing rejection uses a physical bound (0.5 mm, against a ~0.3 mm retina) and an isotropy check rather than per-vendor ranges, so the micrometre case is caught now without the data that `docs/07` item 6 still waits on | Anurag Yadav |
| 2026-09-17 | Document-integrity gates implemented: TC-105 (hash of `docs/01` §2) and TC-106 (image digests match SOUP-014 and SOUP-017). Closes `docs/03` open item 6 and `docs/04` open item 9. | TC-105, TC-106 | TC-105 records a hash of the indications for use, so widening them fails the suite and the only way to pass is a deliberate hash update a reviewer sees. It forces review rather than proving re-classification — `docs/07` §7.3 states that limit and the test's own comment repeats it. The section text is normalised to LF before hashing, because `core.autocrlf` would otherwise fail the gate for a reason unrelated to the intended use. Both gates include a test that the check itself fires, since an assertion never shown to fail has not been shown to work | Anurag Yadav |
| 2026-09-17 | **HD95 definition corrected — values change.** **Old definition:** pool the two directed surface-distance sets into one distribution and take its 95th percentile, i.e. `percentile(concat(d(A→B), d(B→A)), 95)`. **New definition:** take the 95th percentile of each direction separately and return the larger, i.e. `max(percentile(d(A→B), 95), percentile(d(B→A), 95))`. Surfaces are the mask minus its connectivity-1 erosion; distances are spacing-aware and in millimetres; both-empty returns 0.0 and exactly-one-empty returns NaN, unchanged. | SRS-032, RC-006 | **How it was found:** TC-120, the back-to-back check against MONAI, disagreed on HD95 for 5 of 12 random mask pairs while Dice agreed on all 12. Computing both candidate definitions by hand showed max-of-directed reproducing MONAI exactly on the failing seeds. **Why max-of-directed is standard:** it is what MONAI, medpy and the segmentation literature compute, so it is what an HD95 reported for RETOUCH here can be compared against. The pooled version produced numbers that looked reasonable and were comparable to nothing published — a worse failure than an obviously wrong value, because nothing about it invites checking. **Why max-of-directed is also the better measure:** the two directions answer different questions — how far predicted boundary is from reference (false-positive excursions) and how far reference is from predicted (missed regions). Pooling averages them into one distribution, so a large error in one direction is diluted by a well-matched other direction whenever that direction contributes more surface voxels. A prediction that follows the reference closely everywhere except one under-segmented region therefore scores better than it should, which is precisely the failure HAZ-001 describes. Taking the max refuses that dilution: the metric is as bad as its worse direction. **Scope of the change:** no result had been produced under the old definition, so nothing needed recomputing. Had any been published, every HD95 figure in `docs/10` would have required regeneration | Anurag Yadav |
| 2026-09-17 | TC-120 added — back-to-back verification of Dice and HD95 against MONAI over randomly generated mask pairs, in a new `docs/07` §9a cross-verification block. Marked `slow`; needs no data. | TC-120 | Every other metric test was written from the same understanding as the code, which leaves the risk of an implementation that is self-consistent, passes every fixture and is wrong. Agreement with an independently written implementation is evidence neither suite gives alone — and it paid for itself immediately by finding the HD95 defect above. Where the two deliberately differ (our both-empty Dice is 1.0 and HD95 0.0, MONAI returns NaN) the divergence is asserted and documented rather than reconciled | Anurag Yadav |
| 2026-09-17 | `CLAUDE.md` §4 amended: MONAI scoped to transforms, networks and losses; metrics are numpy/scipy by exception, with the reason recorded in `eval/metrics.py`. | — | `docs/10`'s credibility rests on the metrics being independently computable — a Dice only reproducible by running this project's own training stack is a weaker claim than one a reader can recompute from a mask and a spacing tuple — and pure array functions are verifiable without a torch runtime. MONAI's implementations are used as an independent cross-check instead | Anurag Yadav |
| 2026-09-17 | `eval/metrics.py` coverage raised from 90% to 100% (statements and branches) after review. | — | All five uncovered regions were reachable error and edge paths, none dead. The material one was `_as_bool`'s integer branch: every fixture used `dtype=bool` while real label arrays arrive from MetaImage as integers, so the untested branch was the production path | Anurag Yadav |
| 2026-09-19 | **Acquisition context that RETOUCH cannot supply is refused or omitted, never filled.** New SRS-062 (caller must supply), SRS-063 (research exception omits, off by default, logged and recorded), SRS-064 (emitted object checked against the published IOD tables). New HAZ-015 and RC-029. `docs/01` L8, `docs/06` §3.3, `docs/11` §10 item 3. | SRS-062..SRS-064, HAZ-015, RC-029, TC-026..TC-028 | The Ophthalmic Tomography Image IOD assumes a real acquisition on a real device with a fundus reference image; RETOUCH is a converted archive with that context stripped. `ImageLaterality` is Type 1 with enumerated values R/L/B and no unknown member, so there is no conformant value to write. **A detectable non-conformance is safer than an undetectable fabrication:** a missing Type 1 attribute is flagged by any validator, while an invented laterality is read downstream as a recorded anatomical fact and DICOM offers no way to mark a value provisional. `docs/01` §2 was deliberately not amended — the per-eye commitment is correct about the software and merely unexercised by this dataset, so the limitation went to §6.1 and TC-105 correctly did not fire | Anurag Yadav |
| 2026-09-19 | `io/dicom_writer.py` implemented. Spacing written to `PixelMeasuresSequence` in the Shared Functional Groups, not as a top-level `PixelSpacing`. Ophthalmic Frame Location established as usage U and omitted with no conformance consequence. | SRS-006..SRS-011, SRS-054, SRS-062..SRS-064 | highdicom has no constructor for this IOD, so the dataset is assembled with pydicom — the highest invention risk in the project. `validate_against_iod` checks the result against highdicom's PS3.3-derived module and attribute tables, which **immediately found two mandatory attributes the design had missed**: `AcquisitionDateTime` (Type 1, unconditional) and the Enhanced General Equipment model and serial. Both were being papered over with empty strings. That is the difference between a conformance claim verified and one remembered, and it is why the check is a test rather than a one-off | Anurag Yadav |
| 2026-09-19 | **`docs/09` coverage is now computed rather than maintained.** Requirement, hazard and control counts are derived from `docs/02` and `docs/05`; the implemented-test count is read from the suite's own `test_TC_nnn_` function names. | — (mechanism, no identifier changed) | Not merely that the figures were corrected, but why the method changed: **a hand-maintained matrix drifts, and this one had drifted in both directions at once.** It claimed 61 software requirements where there were 64, asserted that `docs/05` did not exist months after it was drafted, listed HAZ and RC ranges that had since grown, overstated risk-control verification at 18 of 29 where the true figure was 17, and understated allocated test cases at 77 where there were 80 — while carrying an explanatory sentence that contradicted its own number. Every one of those errors was introduced by a careful edit. The matrix has therefore stopped being a **claim about** the repository and become an **observation of** it: the only way to change the coverage figures is now to change the code or the documents they are read from. TC-104 automates the regeneration; until it is written, running the regeneration by hand is the fallback | Anurag Yadav |
| 2026-09-19 | `report/seg_object.py` and `report/sr_object.py` implemented via highdicom. SEG carries coded segment descriptions and references its source instances; SR carries per-class volume in mm³ with **coded UCUM units**, the predicted voxel count as a **real NUM measurement** with unit `{counts}`, the voxel volume, and the scan-level MC-dropout confidence. | SRS-038, SRS-039, SRS-040, SRS-041, SRS-058, SRS-059; RC-009, RC-013, RC-025, RC-026 | Units are coded rather than written as text because a consumer that has to guess whether a number is mm³ or µm³ is HAZ-012 arriving by a second route. The voxel count is a NUM content item rather than a comment because RC-026 requires the derivation to be auditable **by machine**: a comment reading \"1000 voxels\" satisfies a reader and not a validator. Coded concepts were checked against pydicom's shipped dictionary: category, anatomic region, the volume measurement name and all three units are verified and used; **no verified code was found for IRF, SRF or PED**, so the segment types are caller-supplied with no default | Anurag Yadav |
| 2026-09-19 | **Frame of Reference added to the Ophthalmic Tomography objects**, with Plane Position and Plane Orientation. | SRS-006, SRS-010 | Found by building the SEG: highdicom refuses a multi-frame Segmentation whose source has no Frame of Reference UID, because the SEG IOD needs a shared spatial frame. The module's condition is not met here, but ends \"May be present otherwise\", so including it is conformant. The UID asserts only that one volume's frames share a frame of reference — true by construction, an identifier minted for a real relationship rather than an anatomical fact invented. Plane Position and Orientation are usage C and **required** in our case since no reference image exists; their values are exact within the self-declared frame. This is a cross-object consequence that only appeared once both objects were actually built | Anurag Yadav |
| 2026-09-19 | **Fluid class coding decided: a declared private scheme, `99OCUVAL`** (`ocuval/report/coding.py`, SRS-065, TC-076), identified in every object that uses it via `CodingSchemeIdentificationSequence` with a scheme UID under the project root. Caller-supplied codes take precedence and suppress the scheme entirely. | SRS-065 (new), RC-029 | Searched pydicom's shipped concept dictionary — 10,751 SCT keywords plus DCM, UCUM and ten other schemes — for all three fluid classes; **nothing verifiable was found for any of them**, while the same search did verify the segment category, retinal region, volume name and all three units, so the absence is a finding rather than a failure to look. Omission is unavailable because the Segmentation IOD requires the segment type, leaving a private scheme or a borrowed approximately-fitting standard code. **The borrowed code was rejected** as the `AnatomicRegionSequence` decision inverted: it looks interoperable while meaning something slightly different to every reader, with nothing marking the approximation. Interoperability cost accepted deliberately — no other system will interpret these codes, which is better than one silently misinterpreting them | Anurag Yadav |
| 2026-09-19 | **TC-107 implemented as a `commit-msg` hook** (`scripts/check_change_control.py`): a commit introducing a new URS/SRS/NFR/HAZ/RC/TC/SOUP identifier in `docs/` must also stage `docs/13`. A second hook rejects a message that claims to change a `docs/` file the commit does not touch. | TC-107, NFR-007 | Prompted by commit `3c16231`, which added SRS-065 with no change control entry because its patch script aborted and the commit proceeded regardless — found by reading the result, not by a check. **The obvious guard would not have caught it:** that commit did touch `docs/11`, so a message-names-untouched-file rule misses it entirely. Measured across all 37 commits then in history, the message rule fires 0 times and the identifier rule fires exactly once, on the incident itself. Both are installed; the evidence says which one is load-bearing. Neither can check that an entry is *true*, which `docs/07` §8 already states | Anurag Yadav |
| 2026-09-19 | `io/dicomweb.py` client internals implemented: URL construction, multipart/related assembly and parsing, STOW-RS response handling, and `trust_env=False` per RC-027. | SRS-043, SRS-045, SRS-060; RC-017, RC-027 | `requests` has no native multipart/related support, so the wire format is hand-written and is the likeliest defect in the module — kept as pure functions and tested without a server. Two behaviours are refusals rather than best-effort: a multipart body with no closing boundary raises instead of returning the parts received so far, because a truncated response yielding usable-looking parts is how a half-retrieved study passes for a whole one; and a STOW-RS failed-instance list is carried through rather than discarded, because a caller ignoring it treats a partial store as complete | Anurag Yadav |
| 2026-09-19 | **The commit-msg guard was configured but not installed**, so it could not fire. `pre-commit install --hook-type commit-msg` now run; `.git/hooks/commit-msg` exists. | TC-107 | Commit `dde5706` repeated the exact failure the guard was written for one commit earlier: `ruff check` failed, the `&&` chain stopped, the `docs/13` update never ran, and the `git commit` on the following line proceeded anyway. The guard did not catch it because a `.pre-commit-config.yaml` entry is inert until the git hook is installed — a control that is specified but not wired is not a control, which is the same distinction `docs/05` §5.3 draws about the other 20 | Anurag Yadav |
| 2026-09-19 | TC-080, TC-081 and TC-082 written for the Orthanc round-trip, and marked in `docs/07` as **written, never executed**. | TC-080, TC-081, TC-082 | Docker is not available in this environment, so no Orthanc existed to run them against. They are written against the DICOMweb specification rather than against observed server behaviour — the better order, since a test written against a live server encodes what that server tolerates — but it means their first run may fail because the client is wrong *or* because the specification requires something Orthanc does not implement, and the second is a `docs/11` finding rather than a defect. Recorded as carrying no evidence until someone reports a run | Anurag Yadav |
| 2026-09-19 | **TC-084, TC-085 and TC-086 allocated** for the DICOMweb client internals, which had been written under the TC-080..TC-082 identifiers belonging to the PACS round-trip. `docs/09` coverage now resolves markers via pytest rather than by grepping for the word. | TC-084, TC-085, TC-086 (new) | Borrowing the round-trip identifiers made three cases that **cannot run without a PACS** appear executed, because their client-side half does run. That is precisely the flattering coverage figure `docs/09` exists to prevent, and it was found by the coverage script disagreeing with itself rather than by review. The marker heuristic had a matching fault: it grepped for `requires_pacs` and so mislabelled TC-083, whose file merely mentions the marker in prose | Anurag Yadav |
| 2026-09-20 | **`no-commit-to-branch` removed** from `.pre-commit-config.yaml`. | — (tooling) | A solo repository with no remote and no review process has nothing for the rule to protect: there is no pull request it would route work through and no second reader it would protect the trunk for. It had also never fired, because the git hooks were not installed until 2026-09-19, so the repository's own configuration had contradicted its practice across more than forty commits. A rule that is both unenforced and routinely violated is worse than no rule — it trains a reader to discount the rest of the configuration | Anurag Yadav |
| 2026-09-20 | `make test` now excludes `requires_data` and `requires_pacs`, matching CI; new `make test-pacs` target runs the PACS tests when an Orthanc is available. | — (tooling) | `make test` invoked bare `pytest`, which since 2026-09-19 collects the eight round-trip tests and fails on connection refused. **A target that fails by design teaches people to ignore failures**, which is the habit this project can least afford. `test` now means everything that can run in this environment | Anurag Yadav |
| 2026-09-23 | **RETOUCH access granted and data on disk.** Registration submitted 2026-09-20 17:35, accepted 2026-09-21; signed Agreement of Data Confidentiality emailed to the organisers 2026-09-20. Training partition only, all three vendors; test partition deliberately not downloaded. Closes `docs/06` open items 1, 3 and 4. | — | The acceptance date is recorded here because `docs/06` §8 makes retention contingent on an authorisation, and an obligation whose commencement is undated cannot be audited. The test partition was declined because it carries no public reference annotations and §1.3 already scopes evaluation to the 70 training volumes — taking data that cannot be used would sit badly with a purpose clause reading "for no other purpose" | Anurag Yadav |
| 2026-09-23 | **`docs/06` §2 rewritten against the signed agreement rather than the rules page, and §2.2's interpretation is no longer claimed to hold intact.** The agreement permits use "solely for evaluating fluid detection and segmentation methods **through the RETOUCH Challenge** and for no other purpose". | — (no requirement changed; a gate reopened) | The earlier interpretation held that publishing a method and results in a public repository counts as participation in the challenge. Two phrases defeat it: "through the RETOUCH Challenge" binds the evaluation to the challenge mechanism rather than its subject matter, and "for no other purpose" closes the gap the rules page left open — a portfolio artefact is a second purpose even when the technical activity is identical. **The activity conforms; the publication may not.** Four options recorded in §2.2, none chosen. `docs/10` and `docs/12` must not be published until this is settled. Recorded as a qualification rather than resolved in the project's favour, which is the error the earlier text made | Anurag Yadav |
| 2026-09-23 | **DMP-C11 and RC-030 added: no reidentification, and no attempt at it.** From the signed agreement. `docs/05` §4.4 records that the control is half technical and half undertaking. | DMP-C11, RC-030 (new) | The technical half is verified — one-way salted-hash pseudonyms whose salt is never committed (SRS-013), so nothing published can be relinked, covered by TC-031. The procedural half cannot be tested: no test shows that someone did not attempt something. Allocating a test to the whole control would overstate what is assured, so the allocation covers the technical half only — the same distinction `docs/03` §4.1 draws about human review | Anurag Yadav |
| 2026-09-23 | **`docs/06` §8 retention rewritten as conditional**, with new §8.1 deletion-on-request. §7 records that access is single-recipient, the fact behind DMP-C5. §4 layout corrected against the extracted archive and new §4.1 documents the directory junction to `D:\RETOUCH_DATA`. | DMP-C5 (basis restated) | The organisers may withdraw authorisation at any time, so the project holds the data at their discretion rather than for a term of its own choosing. That is a design constraint, not paperwork: every derived stage must stay reproducible from the one before it, and no result may depend on data that might have to be destroyed before the result is reproduced. §8.1 names deleting the junction *target* rather than the link, which would otherwise leave the archive in place while appearing to remove it | Anurag Yadav |
| 2026-09-23 | **`docs/06` §3.2 prevalence figures corrected against the archive; every fluid-class value was wrong.** Measured pooled over all 70 reference volumes. Cirrus IRF 0.04→0.4019 (10.0× low), Spectralis IRF 0.15→0.5821 (3.9×), Topcon IRF 0.03→0.2043 (6.8×); SRF understated 1.5–2.0× in every vendor; PED **over**stated 1.5–2.2× in every vendor. Foreground range 0.55–1.26% → **0.60–1.48%**. `docs/01` L2 and L3 and the `eval/metrics.py` docstring updated to match. | — (figures; no requirement text changed) | Rule 6. The pooled voxel count and the mean of per-volume percentages agree, so this is not a difference of statistic — the published figures were simply wrong. The error is **systematic**: IRF and SRF low in all three vendors, PED high in all three, which suggests the source described something other than voxel-wise prevalence over the training partition rather than noisy transcription. The source was not re-identified; measured values govern. **No requirement needed revisiting**: the accuracy prohibition depended on a null predictor scoring far above chance and it scores 98.52–99.40%, and background-excluded Dice loss depended on the imbalance existing, which it does more strongly than recorded | Anurag Yadav |
| 2026-09-23 | **`docs/06` §3.1 Topcon geometry corrected.** No Topcon volume has ~122 B-scans: 20 have 128 and 2 have 64, across three geometries — 512×650×128 (10), 512×885×128 (10), 512×885×64 (2). Subject counts 24/24/22 and all slice totals were correct. New §3.1.1 records element types and §3.1.2 records measured per-vendor spacing. Closes `docs/06` open item 2. | — (figures) | The ~122 figure was 2,688 / 22 — a mean over a bimodal distribution, presented as though it described a volume. **It survived because every aggregate derived from it was correct**, which is what makes an average-as-typical error durable. Two consequences beyond the number: heterogeneity is now known to be intra-vendor as well as cross-vendor, making SRS-010 and SRS-026 more load-bearing than when written; and Spectralis `oct.raw` is MET_USHORT while Cirrus and Topcon are MET_UCHAR, so the reader must take element type from the header rather than assume it, on the same grounds SRS-003 gives for spacing | Anurag Yadav |
| 2026-09-23 | **`docs/06` §3.2.1 added: 58 of 70 volumes lack at least one fluid class**; only 12 contain all three. IRF present in 55, SRF in 37, PED in 33; no volume is empty of all fluid. | — (dataset characteristic) | This makes the empty-mask conventions the common case rather than an edge case, and they are exercised on most per-class evaluations. It vindicates the 2026-09-17 decision that `hd95` returns NaN rather than inf when exactly one mask is empty: on this data inf would have destroyed the majority of per-class HD95 aggregates. It also makes `bootstrap_ci` reporting the n actually used load-bearing rather than tidy — a PED HD95 could otherwise be quoted over a handful of volumes and read as covering 70 | Anurag Yadav |
| 2026-09-23 | **Scope question resolved by the challenge organiser; `docs/06` §2.2.1 added and the §2.4 decision gate closed as not triggered.** The author wrote to Dr Hrvoje Bogunović on 2026-09-23 asking whether publishing the method and results in a public repository, with no data redistributed, falls within the Agreement's permitted use; he confirmed the same day that publishing results outside the challenge itself is fine. `docs/10` and `docs/12` unblocked. The online submission stays planned but is no longer a dependency of publication. | — (permission recorded; no requirement changed) | Rule 6. **The general finding, which outlasts the specific answer:** the binding document was narrower than the public rules page, so reasoning from the rules page had been reasoning from the wrong text; our reading of the *narrower* wording then turned out to be **more restrictive than the rights-holder's own**; and the matter was settled by asking the party the obligation is owed to, in one day, rather than by further analysis. Recorded as **superseding** §2.2 rather than confirming it — §2.2 concluded the publication might not be permitted and that conclusion was wrong, so it is retained as the reasoning that prompted the question rather than as a position still held. Careful reasoning about someone else's document can only ever produce a hypothesis about their intent | Anurag Yadav |
| 2026-09-23 | **`io/retouch_reader.py` completed and new `io/metaimage.py` added**; SRS-066 allocated: the pixel element type is taken from the header and carried unchanged, never cast to a fixed depth. `dicom_writer` now derives Bits Allocated from the array dtype. TC-017 and TC-018 allocated. | SRS-066, TC-017, TC-018 (new) | Spectralis volumes are 16-bit and Cirrus and Topcon are 8-bit (`docs/06` §3.1.1). The writer previously cast everything to uint16, which is lossless and still wrong: every object would declare a precision its acquisition never had, and a consumer reading Bits Stored could not distinguish a genuine 16-bit scan from a padded 8-bit one. PS3.3 C.8.17.7 enumerates Bits Allocated as 8 or 16 and Bits Stored as 8, 12 or 16, verified against the standard, so carrying both source depths is conformant rather than a workaround. `metaimage.py` is separate from `retouch_reader.py` so the file format and the dataset layout are two concerns, and deliberately does not use SimpleITK: `docs/04` SOUP-005 records that its resampling API makes the harmonisation SRS-010 and SRS-026 forbid easy to do by accident, and not importing it removes the possibility rather than relying on care | Anurag Yadav |
| 2026-09-23 | **First end-to-end run on real data**: 70 volumes converted, de-identified and split. No training. | — (execution, no requirement changed) | Every volume triggered the SRS-063 research exception, omitting all five unpopulatable mandatory attributes — 70 of 70, exactly as designed and never once filled. De-identification verified clean on all 70 with 70 distinct pseudonyms and the vendor surviving in every case (RC-008). The three folds resolved and `assert_no_patient_overlap` passed against the real manifest. Recorded because it is the first evidence any of this works on data rather than fixtures | Anurag Yadav |
| 2026-09-23 | **Spacing axis-order conversion identified as an untested path, and `spacing_from_header` written with tests to cover it.** `docs/07` §3 gains rule 7 on fixtures and conversions. | SRS-003, SRS-054; HAZ-012 | **Mechanism.** MetaImage orders `ElementSpacing` as (x, y, z), which for RETOUCH is (lateral within a B-scan, axial, separation between B-scans). This project orders `spacing_mm` as (axial, lateral, separation), because within a frame the rows run axially and the columns laterally. Ingestion must therefore swap the first two components. **Why it was untested.** Every synthetic fixture in the suite was written in the project's own (axial, lateral, separation) order, so the fixtures exercised every downstream consumer of spacing and never the conversion itself — the conversion did not exist until real data arrived, and no fixture would have exercised it if it had. **Consequence had it been wrong.** For Cirrus, lateral 0.011742 mm against axial 0.001955 mm, so a transposed pair yields a volume in mm³ wrong by a factor of **6.0** — from a segmentation that is entirely correct, with nothing visible in the mask, the image or the review screen. That is HAZ-012 precisely, and `docs/05` §5.4 explains why no downstream control reaches it. **General lesson, recorded in `docs/07` §3 rule 7 rather than only here: a fixture written in the consuming code's convention cannot test a conversion.** The fixture must be in the source convention and the assertion in the target one, the conversion must be non-identity on the fixture, and the test should assert a property the target convention implies — here, that axial ends up the finest of the three | Anurag Yadav |
| 2026-09-23 | `docs/11` §10 item 3 records that the SRS-063 research exception fired on **70 of 70** volumes, omitting all five unpopulatable attributes in every one. | SRS-062, SRS-063 | The non-conformance list alone reads as though the omissions were occasional. For RETOUCH-derived data the strict path of SRS-062 is **unreachable**: it is implemented and tested by TC-026 and will never execute on this dataset, because the caller has nothing to supply. A reader assessing conformance should assume every object is missing all five rather than treating it as a per-volume question | Anurag Yadav |
| 2026-09-23 | **SRS-067, SRS-068, SRS-069, RC-031 and TC-043 added: volume-to-frame expansion is now a specified, tested stage.** HAZ-008 gains a second causal mechanism; `expand_to_frames` and `assert_no_patient_overlap_after_expansion` implemented in `data/splits.py`. | SRS-067, SRS-068, SRS-069; HAZ-008; RC-031; TC-043 | **The gap.** `docs/02` §2 defined a Sample as one B-scan, and splitting operates on volumes — a manifest row and a DICOM instance are both one acquisition. Nothing in the requirements said how the first is derived from the second. The DICOM layer writes one multi-frame instance per volume while training consumes 2-D frames, so an expansion step must exist, and it was going to be written inside the datamodule where no requirement and no test reached it. **Why it is a leakage path and not a convenience.** HAZ-008 previously had one mechanism: the split itself assigns a patient to two buckets, which TC-004 gates in CI. A split can be correct at volume level and still leak if expansion misassigns frames, and TC-004 inspects the volume-level split, so it would pass. That is a second, independent route to the same optimistic Dice, added to HAZ-008 rather than raised as a new hazard because the harm and the sequence after the misassignment are identical. **The control.** RC-031: expansion happens only after splitting, frames inherit and never acquire an assignment, `ocuval.data.splits` is the only module permitted to perform it, and the result is **asserted** disjoint at patient level after expansion rather than assumed to have inherited correctly. The assertion is the part that matters — inheritance is easy to write and easy to get wrong, and checking the output costs one pass over the frames | Anurag Yadav |
| 2026-09-25 | **History rewritten to remove co-author trailers; nine dangling SHA citations across four files remapped; TC-108 allocated to catch the next one.** | TC-108; NFR-008 | **Cause, plainly.** Rewrite 2026-09-23, remediation 2026-09-25. On 2026-09-23 the 51-commit history was rewritten with `git filter-branch --msg-filter` to delete the `Co-Authored-By:` trailer from the 44 commit messages carrying one, and force-pushed. No content changed — every rewritten commit reuses the original tree object, all 51 tree hashes were verified identical pairwise against `backup/pre-trailer-strip`, and `git diff` between old and new tips is empty. But a commit's SHA covers its message, so **every SHA from the root commit onward changed**, and the citations naming them were left pointing at commits no longer reachable from `main`. **Scope.** Nine occurrences of four distinct SHAs across four files: `945705d` (was `35027a3`) and `56a30b3` (was `8c2086b`) in `docs/13`; `dde5706` (was `1a9d75b`) in `docs/13`; `3c16231` (was `104342b`) six times, in `docs/07` §8, `docs/13`, `scripts/check_change_control.py` and `.pre-commit-config.yaml`. The first scan for these was scoped to `docs/`, `scripts/`, `src/` and `CLAUDE.md` and reported six occurrences in three files; it missed `.pre-commit-config.yaml` at the repository root. The figure here is the repo-wide one. **Why `3c16231` mattered most.** It is the incident that `scripts/check_change_control.py` exists to catch and cites as its own justification, in its docstring, in the pre-commit config and in `docs/07` §8 under TC-107. A guard whose stated evidence does not resolve is a guard a reviewer cannot check. Both citations now point at the new SHA and each file records that the reassignment happened, so a reader holding a pre-rewrite clone is not left guessing. **TC-108.** Every commit SHA cited in `docs/`, `scripts/`, `CLAUDE.md` or `.pre-commit-config.yaml` must resolve to a commit reachable from `main`. Implemented in `tests/test_document_integrity.py`, wired into pytest and therefore into CI — **this is the third time a control has been specified without being wired** (RC coverage, `docs/05` §5.3; TC-107 inert until `pre-commit install --hook-type commit-msg`), and a dangling citation is exactly the class of defect it should catch, so it is a running test and not a note. It is negative-tested: reintroducing `104342b` fails it naming the SHA and the file | Anurag Yadav |
| 2026-09-25 | **Guidance recorded: prefer a description plus a date over a bare SHA.** | NFR-008 | A commit SHA is not a stable identifier. It covers the message as well as the tree, so any rewrite — trailer removal, a rebase, a squash, an author correction — invalidates every SHA from the rewritten commit onward, while the commit it names still exists and still means the same thing. The citation breaks even though nothing about the fact being cited changed. **Where the identity of the specific commit is not itself the point**, cite it as "the commit of 2026-09-19 that added SRS-065 without a change control entry" rather than as `3c16231`: that description survives any rewrite and is what a reader needs in order to find it. **Where the SHA is the point** — a configuration item, a release, a reproduction — keep it, and accept that TC-108 is what tells you when it has gone stale. TC-108 catches the breakage; this guidance reduces how often there is breakage to catch | Anurag Yadav |
| 2026-09-25 | **SRS-014 reworded: a pseudonym must be a deterministic function of the identifier and salt alone, in every run and on every machine.** A persistent salt was created, `docs/06` §5.1 and §5.2 record its existence and location, and the pipeline was re-run under it. TC-032 extended. | SRS-014; TC-032 | **The defect was in the requirement, not the code.** SRS-014 said pseudonyms "shall be stable within a run", and gave the reason: patient-level splitting must stay valid after de-identification. That reason is real but it is not the only one, and a requirement stating only the weaker property permits an implementation that satisfies it and destroys provenance — a fresh random salt per run passes "stable within a run" exactly. Two runs would then issue different pseudonyms for the same subject, so a result recorded in one could not be matched to the volume it came from in another. URS-010 exists for that matching. **`pseudonymise` already computed `sha256(salt:patient_id)`**, a pure function, so the practice was correct throughout and no object was ever mis-derived; what was wrong was that nothing required it to stay that way. Fixing only the practice, by generating a salt and moving on, would have left the next implementer free to reintroduce the ephemeral one against a requirement that still permitted it. **Consequence accepted and recorded rather than hidden:** the 2026-09-23 run read an ephemeral salt, so every pseudonym it issued is unreproducible. Nothing depends on them — that run was superseded and no result was recorded against it — but `docs/06` §5.2 states it rather than letting the re-run quietly replace the numbers. **Verification** is five checks against the written instances rather than the run's own bookkeeping, including re-deriving each pseudonym from the stored salt after reading it back off disk, and confirming the salt value appears in no object. **TC-032** now asserts the pseudonym against an independently computed digest, and asserts agreement from a separate interpreter with `PYTHONHASHSEED` set differently, so a derivation accidentally depending on `str.__hash__` or on any per-process state cannot pass | Anurag Yadav |
| 2026-09-25 | **Per-vendor spacing plausibility ranges measured, configured and enforced; `docs/07` open item 6 closed and its evidence recorded in a new `docs/07` §13.** `validate_spacing` now takes the vendor; both call sites pass it. | SRS-056; TC-015; HAZ-012, RC-023 | **No new requirement.** SRS-056 has required per-vendor ranges in `configs/data.yaml` since the SRS was written; what was missing was the data to define them, and the ranges were owed rather than undecided. **Measured** from all 70 training volumes, in the project's (axial, lateral, separation) order. **Margin of ×2 chosen by measurement, not taste:** at ×2 the ranges accept 70/70 real volumes and reject an axial/lateral transposition on 70/70; at ×3 transposition detection on spectralis collapses to 3/24, because that vendor's axial and lateral ranges begin to overlap. Two is inside the working band with headroom either side. The margin exists at all because the extremes are the training partition's, not the population's — the test partition was deliberately not taken (`docs/06` §2.1). **Which bound rejects, measured across every realistic defect class rather than asserted:** the outer 0.5 mm bound catches the ×1000 micrometre error 70/70 and **zero** of the 210 axis-order cases; the per-vendor range catches all 210 plus 46 of the 70 ×10 errors; isotropy is caught structurally; nothing is missed by every bound. **This is the significant finding.** A transposed spacing is physically small in every component — 0.011742 mm is a plausible OCT number, just the wrong axis's — so no physical bound can see it. Before today the only thing between that defect and a reported volume in mm³ was `spacing_from_header` being written correctly, and `docs/13` (2026-09-23) records that the error would be a factor of six with nothing visible in the mask, the image or on a review screen. There is now an independent second check on the class of defect this project has already found once. **Limits recorded in §13.4:** these are what the archive contains, not vendor specifications; an unmeasured vendor falls back to the physical bound rather than being refused, and TC-015 asserts the configured vendor set matches `vendors:` so a vendor cannot silently drop to the weaker bound | Anurag Yadav |
| 2026-09-25 | **Milestone 5 preprocessing specified and implemented: SRS-070..SRS-074, TC-044..TC-048, `data/transforms.py` and `data/datamodule.py`.** Declared constants added to `configs/train_seg.yaml`; `OCTVolume` carries an intensity window computed at ingestion. | SRS-070, SRS-071, SRS-072, SRS-073, SRS-074; TC-044..TC-048; RC-031 | **Normalisation: per-volume percentile window, and why not dtype scaling.** Dividing by 255 or 65535 normalises the container, not the signal. Measured across the training partition, median as a fraction of full scale is 0.122 cirrus, 0.132 spectralis, **0.235 topcon** — two vendors coincide and the third is separated by nearly a factor of two, because topcon carries a raised black level of **p1 = 35/255** against 0 for the other two. That is a detector offset, not tissue, and under dtype scaling it survives as a constant brightness offset perfectly correlated with vendor: the most learnable shortcut in the dataset, and one that would inflate in-domain Dice and collapse on the held-out vendor. A percentile window removes gain and offset together; verified to remove a clean +35 pedestal **exactly**, and to make 8-bit and 16-bit encodings of one signal agree to within the 1/255 quantisation floor. **Computed at ingestion, not in the transform.** For 2D training the array a transform receives is one B-scan, so a percentile taken there would be a per-frame statistic, giving adjacent B-scans different windows and injecting an intensity gradient along the volume that the acquisition does not contain. The window is therefore computed once in `read_volume`, stored on the record and in the manifest, and `ApplyStoredIntensityWindowd` raises rather than falling back to computing one. **Inheritance is asserted, not conventional** (SRS-071, TC-045): `assert_window_inheritance` checks every frame against its volume's row, on the same reasoning as `assert_no_patient_overlap_after_expansion` — the lookup is easy to get subtly wrong and the failure is invisible, because a frame normalised against another volume's window is still a plausible B-scan. **Geometry: axial only, to a declared coarsest target.** Lateral spacing is ~0.0117 mm in all three vendors and is untouched. B-scan separation is not read by the chain at all — training is 2D and frames are independent, and SRS-073 states this so that enabling 2.5D is a visible decision rather than a quiet one. The target is **0.004 mm declared in `configs/train_seg.yaml`**, not derived: a target computed from whichever volumes are in a fold would make preprocessing depend on fold membership and stop being comparable between folds. It is at or above every observed axial spacing, so nothing is ever upsampled — downsampling discards information, upsampling fabricates it, and fabricated detail would substitute a smoothness signature for the brightness signature just removed. Rounding to 0.004 rather than spectralis's 0.003872 also avoids one vendor passing through with no interpolation at all. All three vendors converge to 576×512, so the chain pads and never crops. **Metrics in native geometry** (SRS-074): predictions are inverted before any metric, because SRS-057 requires the spacing used for a volume in mm³ to be bit-identical to ingestion's, which after resampling it is not. **Recorded as a limitation, not only as a change:** `docs/10` §8.1 states that the measured cross-vendor gap is a **lower bound** on the raw-data gap, names the topcon pedestal as the specific thing removed, and gives the argument for why removing it is defensible rather than convenient — a gap driven by an uncorrected detector offset would be evidence of a preprocessing bug rather than evidence about transfer | Anurag Yadav |
| 2026-09-25 | **CI fixed: `actions/checkout` now uses `fetch-depth: 0`, and TC-108 distinguishes a shallow clone from a stale citation.** | TC-108; NFR-008 | **The failure.** The `quality` job failed on `main` from the commit that introduced TC-108 onward. Reproduced exactly by cloning the published repository with `--depth 1` and running CI's own command: **313 passed, TC-108 the only failure**, reporting all four cited SHAs as unresolvable. **The diagnosis, and what it was not.** `actions/checkout@v4` defaults to `fetch-depth: 1`, so the checkout contains one commit and `git rev-list main` returns one SHA. The citations were correct; the clone had no history to resolve them against. It was **not** an unmarked data-dependent test — the same reproduction confirms every data- and PACS-dependent test was correctly marked and deselected, and no test touched `data/` or the junctions without a marker. **The fix, and why not the other one.** Fetching full history, not skipping the check on a shallow clone. A skip would mean the guard silently stops running in exactly the environment it exists to protect, which is the specified-but-not-wired failure this project has already hit three times. TC-108 additionally now **asserts** the repository is not shallow, with a message naming `fetch-depth`, so a future regression of the workflow reports its own cause instead of listing four correct SHAs as though they were wrong — the two failures have opposite remedies and should not look alike. **Verified both ways:** on a shallow clone the suite fails with the shallow-clone message; on the same clone after `git fetch --unshallow`, 314 pass | Anurag Yadav |
| 2026-09-25 | **Finding: a physical plausibility bound cannot detect an axis-order error. Only the per-vendor ranges can.** Recorded as a finding in its own right, separately from the entry that closed `docs/07` item 6. | HAZ-012; RC-023; SRS-056; TC-015 | **The measurement.** Each of the 70 real spacings was perturbed by each realistic defect class and passed to `validate_spacing`, recording which bound fired. The 0.5 mm physical bound caught the ×1000 micrometre error on 70 of 70, and **0 of 210** axis-order cases across the three permutations (axial/lateral, lateral/separation, full reversal to MetaImage order). The per-vendor ranges caught all 210. **Why the physical bound is structurally blind to this.** It asks whether a number could be a retinal OCT voxel edge. In a transposition every number still could: cirrus lateral spacing is 0.011742 mm, which is an entirely plausible OCT value — it simply belongs to a different axis. There is no threshold on a single component that separates "plausible value in the right place" from "plausible value in the wrong place", because the defect is not in the values at all. It is in their **order**, and only a bound that knows what each axis should look like *for this vendor* can see it. **Why it matters more than its share of the table.** This is HAZ-012 exactly: `docs/13` (2026-09-23) records that a transposed cirrus pair yields a volume in mm³ wrong by a factor of **6.0** from a segmentation that is entirely correct, with nothing visible in the mask, the image or on a review screen, and `docs/05` §5.4 explains why no downstream control reaches it. **Before 2026-09-25 the sole guard against it was `spacing_from_header` being written correctly** — one function, one line of axis permutation, verified by tests written the same day by the same author, with no independent check anywhere in the pipeline. The per-vendor ranges are that independent check, and they were obtained as a by-product of closing an open item rather than by looking for this. **Consequence for future changes:** the per-vendor ranges must not be relaxed to the physical bound alone on the grounds that the physical bound "already covers spacing". It does not cover this, and this is the defect class the project has already hit once | Anurag Yadav |
| 2026-09-25 | **`scripts/01`, `02` and `03` implemented; the pipeline re-run through them; the 2026-09-23 ad-hoc artefacts superseded.** New module `ocuval/runs.py` holds config resolution, provenance and the manifest. TC-049 allocated. | SRS-031; SRS-023; TC-049; HAZ-004 | **What was wrong with the first run, stated rather than replaced quietly.** The 2026-09-23 end-to-end run — 70 volumes converted, de-identified and split — was driven by an **ad-hoc script in a scratch directory**, with its manifest written there too. The artefacts were real and correct, and TC-049 has since confirmed the DICOM matches its MetaImage source, but **they could not be regenerated from anything in the repository**. That contradicts SRS-031, which requires the seed, resolved configuration and version in the run output, and the determinism argument in CLAUDE.md rule 4 that the whole project rests on. A result nobody can reproduce is not a result. **What the scripts now do.** Each takes `--config`, resolves it, and writes `run.json` beside its output **at the start** of the stage, so a run that aborts halfway still records what it was attempting — a provenance file written on success is absent exactly when it is wanted. The manifest is a stage output under `data/`, sorted and written with sorted keys so two runs over one archive produce a byte-identical file. Stage 02 records a **fingerprint** of the salt, never the salt. Stage 03 asserts no patient overlap **before** anything reaches disk, because a file on disk is what a later run will trust. **Verified against the superseded run:** pixel data identical 70/70, spacing identical 70/70, intensity window identical 70/70, pseudonym identical 70/70. **The scratchpad manifest was deleted**, so there is one source. **One divergence, and it is a defect the re-run exposed rather than caused:** SOP Instance UIDs changed on all 70, because `generate_uid` is random per run. The split files key off `sample_id`, which is the SOP Instance UID, so **the split files are not byte-reproducible across conversions** even though the patient partition is identical on all three folds. SRS-023 requires a split to be reconstructible from the committed configuration alone, and it currently is not. Recorded here as an open defect; the fix is a decision about DICOM identity — whether re-converting one acquisition is the same instance — and is not being made unilaterally | Anurag Yadav |
| 2026-09-25 | **SRS-023 corrected: split files are keyed on the source subject identifier, not the SOP Instance UID.** `splits.py` and `datamodule.py` re-keyed; TC-041 extended; TC-004 and TC-043 fixtures updated to the new semantics. | SRS-023; TC-041, TC-004, TC-043 | **The defect, and why the key was wrong rather than the UID.** A split partitions **subjects**, so an instance identifier was never the right key. `generate_uid` mints a UID per conversion, so re-converting the archive produced a split file differing in all 70 entries while partitioning exactly the same subjects — verified on all three folds. The partition was reproducible; the file was not, and SRS-023 asks for the file. **UIDs stay random.** A re-conversion is a new instance under DICOM semantics, and deterministic UIDs would make separately created objects share an identity — a worse error than the one being fixed, and one that would travel outside this repository. The reproducibility failure was a symptom of the key, not of the UID policy. **Why not the pseudonym or the path.** The pseudonym depends on a salt that is deliberately never committed (`docs/06` §5.1), so a split keyed on it could not be regenerated by anyone without the salt — it would trade one irreproducibility for another. A path is per machine. **Why committing the case label is not redistribution.** `TRAIN001`..`TRAIN070` are the organisers' own anonymous labels, assigned by them for the challenge. They are dataset *structure*, not dataset *content*: they carry no pixels, no annotation, no clinical fact and no subject attribute, and they are already implied by any published per-case result. `docs/06` §2 governs redistribution of the data itself and is not engaged. **A structural gain, not only a reproducibility fix.** The old key made a leak representable — two instances of one subject in different buckets, caught only by the patient-level assertion. Under the new key one subject is one entry in exactly one bucket, so that arrangement cannot be written down. TC-004 gains a test for the structural property and one asserting a split written against the old key is **refused** rather than read as unknown one-volume patients, which would have passed every disjointness check while referring to nothing. **Verified:** three folds written twice, byte-identical both times; partition identical to the pre-change one on all four buckets of all three folds; 321 tests pass | Anurag Yadav |
| 2026-09-25 | Commit `84d9ae3` carries two concerns: the CI `fetch-depth` fix and the TC-049 correspondence test. | — | Noted rather than corrected. CLAUDE.md §6 asks for one concern per commit and this commit has two, because `git add -A` swept in work that was in progress when the CI failure arrived. History is not being rewritten to split it: the 2026-09-23 rewrite is recent, the record of it is still being relied on by TC-108's `RETIRED_SHAS`, and a second rewrite to tidy a commit message would cost more in broken citations than the untidiness costs. Recorded so the deviation is visible rather than silent | Anurag Yadav |
